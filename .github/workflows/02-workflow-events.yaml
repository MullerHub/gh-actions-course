name: 02 - Workflow Events

on:
  workflow_dispatch:
jobs:
  echo:
    runs-on: ubuntu-latest
    steps:
      - name: Show the trigger
        run: echo "O evento que disparou este workflow foi; ${{ github.event_name }}"
